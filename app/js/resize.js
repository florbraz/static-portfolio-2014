App.Resize = {
	Smart: function(evt){

		// calls functions that need to be triggered on resize. they'll only be triggered when the user releases
		
	},
	Force: function(evt) {
		// put on force functions that you want to be triggered as much as the user resizes the page. not recomended!!!
	}
}